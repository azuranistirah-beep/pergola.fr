// Prisma schema - MySQL (Hostinger)
// Full domain schema will be modeled in Tahap 3.
// This file is intentionally minimal at Tahap 2 so `prisma generate` works.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
